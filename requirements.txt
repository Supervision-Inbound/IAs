# requirements.txt (Python 3.11 compatible para re-entrenamiento v29)

# --- Core ML Libraries ---
scikit-learn==1.2.2
pandas==1.5.3
# numpy < 1.24 es a menudo requerido por TF 2.12
numpy==1.23.5
# TensorFlow compatible con Python 3.11
tensorflow==2.12.0
joblib==1.2.0

# --- Otras dependencias ---
requests>=2.31
requests-cache>=1.1
python-dateutil>=2.8
tqdm>=4.66
retry-requests>=2.0
# Necesario para leer Excel en pandas
openpyxl

# --- Añade aquí cualquier otra librería específica ---
